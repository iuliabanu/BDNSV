apiVersion: 1
datasources:
- access: proxy
  editable: true
  isDefault: true
  jsonData:
    httpMethod: POST
    queryTimeout: 60s
    timeInterval: 5s
  name: Prometheus
  type: prometheus
  url: http://prometheus:9090
